'use strict';

import ArrowButton from '../../UI/ArrowButton/ArrowButton.vue'

export default{
    name: 'Subscribe',
    components: {
        ArrowButton,
    },
    data() {
        return {
            
        }
    },
}